<Window x:Class="MovieWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:nx="http://schemas.nuext.minimal/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MovieWpfApp"
        xmlns:viewModels="clr-namespace:MovieWpfApp.ViewModels"
        xmlns:views="clr-namespace:MovieWpfApp.Views"
        xmlns:controlzex="urn:controlzex"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance {x:Type viewModels:MainWindowViewModel}, IsDesignTimeCreatable=True}"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen"
        Title="{Binding Title, TargetNullValue={x:Static local:Loc.Untitled}}" Height="450" Width="800" MinHeight="450" MinWidth="800">
    <nx:Interaction.Behaviors>
        <nx:WindowService/>
        <nx:InputDialogService MessageBoxButtonLocalizer="{StaticResource MessageBoxButtonLocalizer}"
                               ValidatesOnDataErrors="True"/>
        <nx:EventToCommand EventName="Closing" Command="{Binding ClosingCommand}" PassEventArgsToCommand="True" />
        <nx:EventToCommand EventName="ContentRendered" Command="{Binding ContentRenderedCommand}"/>
        <nx:EventToCommand EventName="Loaded" Command="{Binding LoadedCommand}" />
        <nx:KeyToCommand Gesture="CTRL+O" Command="{Binding ShowMoviesCommand}" />
        <nx:WindowPlacementService FileName="MainWindow" 
                                   DirectoryName="{Binding EnvironmentService.SettingsDirectory, FallbackValue={x:Null}}"
                                   PlacementRestoredCommand="{Binding PlacementRestoredCommand}" 
                                   PlacementSavedCommand="{Binding PlacementSavedCommand}"/>
        <nx:SettingsService Prefix="MainWindow" 
                            DirectoryName="{Binding EnvironmentService.SettingsDirectory, FallbackValue={x:Null}}"/>
        <nx:WindowedDocumentService x:Name="Windows" 
                                    ActiveDocument="{Binding ActiveWindow}"
                                    FallbackViewType="{x:Type views:ErrorView}">
            <nx:WindowedDocumentService.WindowStyle>
                <Style TargetType="{x:Type Window}">
                    <Setter Property="nx:Interaction.BehaviorsTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <ItemsControl>
                                    <nx:WindowService Name="CurrentWindowService"/>
                                    <nx:EventToCommand EventName="ContentRendered" Command="{Binding ContentRenderedCommand}"/>
                                </ItemsControl>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </nx:WindowedDocumentService.WindowStyle>
            <nx:Interaction.Behaviors>
                <nx:EventToCommand EventName="ActiveDocumentChanged" Command="{Binding ActiveWindowChangedCommand}"/>
            </nx:Interaction.Behaviors>
        </nx:WindowedDocumentService>
    </nx:Interaction.Behaviors>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" ItemsSource="{Binding MenuItems}" ItemContainerStyle="{StaticResource Styles.MenuItem.Default}"/>

        <controlzex:TabControlEx Grid.Row="1" TabStripPlacement="Top">
            <nx:Interaction.Behaviors>
                <nx:TabbedDocumentService x:Name="Documents"
                                          ActiveDocument="{Binding ActiveDocument}"
                                          FallbackViewType="{x:Type views:ErrorView}">
                    <nx:Interaction.Behaviors>
                        <nx:EventToCommand EventName="ActiveDocumentChanged" Command="{Binding ActiveDocumentChangedCommand}"/>
                    </nx:Interaction.Behaviors>
                </nx:TabbedDocumentService>
            </nx:Interaction.Behaviors>
        </controlzex:TabControlEx>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock>
                    <Run Text="{x:Static local:Loc.Documents_Opened}"/>
                    <Run Text=":"/>
                    <Run Text="{Binding Count, ElementName=Documents, Mode=OneWay}"/>
                </TextBlock>
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <TextBlock>
                    <Run Text="{x:Static local:Loc.Windows_Opened}"/>
                    <Run Text=":"/>
                    <Run Text="{Binding Count, ElementName=Windows, Mode=OneWay}"/>
                </TextBlock>
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <TextBlock Text="{Binding Source={StaticResource AppBindingProxy}, Path=DataContext.PerformanceMonitor.FormattedUsage, FallbackValue={x:Null}}" />
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>
