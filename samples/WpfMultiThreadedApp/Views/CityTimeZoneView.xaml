<UserControl x:Class="WpfMultiThreadedApp.Views.CityTimeZoneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WpfMultiThreadedApp"
             xmlns:viewModels="clr-namespace:WpfMultiThreadedApp.ViewModels"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance {x:Type viewModels:CityTimeZoneViewModel}, IsDesignTimeCreatable=True }"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Viewbox Margin="10">
            <ContentControl x:Name="TimeContent" Content="{Binding}" ContentTemplate="{StaticResource DateTimeProgressTemplate}"/>
        </Viewbox>

        <StatusBar Grid.Row="1">
            <StatusBarItem>
                <TextBlock>
                    <Run Text="{x:Static local:Loc.Thread_Id}"/>
                    <Run Text=":"/>
                    <Run Text="{Binding Source={x:Static sys:Environment.CurrentManagedThreadId}, StringFormat='{}{0}', Mode=OneTime}"/>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
